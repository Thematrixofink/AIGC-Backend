# 线上配置文件
server:
  port: 8081
spring:
  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/aigc
    username: root
    password:
  # Redis 配置
  redis:
    database: 1
    host: localhost
    port: 6379
    timeout: 5000
    password:

  #rabbitMQ配置
  rabbitmq:
    host: localhost
    port: 5672
    password: guest
    username: guest

mybatis-plus:
  configuration:
    # 生产环境关闭日志
    log-impl: ''
